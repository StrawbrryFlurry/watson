import { IReturnValue } from './interfaces';

/**
 * Sends the message back to the user that executed the command.
 * Has the same functionality as using the `reply` method on the DiscordJS Message object
 */
export function toReply(value: IReturnValue) {}
