import { User } from 'discord.js';

/**
 * Use this helper to mention a user.
 * @param user The user object or the user id to be mentioned
 * @returns The the string to mention a user.
 *
 * @example
 * ```
 * declare const user: User;
 * const message = `Hey, ${mention(user)}!`
 *```
 */
export function mention(user: User | string) {}
